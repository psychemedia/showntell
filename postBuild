#!/bin/bash

R --quiet -e "install.packages(c('stringr', 'reshape2', 'ggplot2'))"

# Notebooks w/ extensions that auto-run code must be "trusted" to work the first time
jupyter trust index.ipynb
jupyter trust index_R.ipynb
